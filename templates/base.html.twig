<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Bem-vindo!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {# Aqui é onde você pode adicionar elementos comuns a todas as páginas, como:
           - Barra de navegação (navbar)
           - Rodapé (footer)
           - Mensagens Flash (feedback ao usuário)
        #}
        <header class="bg-blue-600 text-white p-4 shadow-md">
            <div class="container mx-auto flex justify-between items-center">
                <div class="text-2xl font-bold">
                    <a href="{{ path('app_task_index') }}" class="hover:text-blue-200">Minha Lista de Tarefas</a>
                </div>
                <nav>
                    <ul class="flex space-x-4">
                        <li><a href="{{ path('app_task_index') }}" class="hover:text-blue-200">Listar Tarefas</a></li>
                        <li><a href="{{ path('app_task_new') }}" class="hover:text-blue-200">Adicionar Tarefa</a></li>
                    </ul>
                </nav>
            </div>
        </header>


        <div class="container mx-auto mt-4">
            {% for message in app.flashes('success') %}
                <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-3" role="alert">
                    {{ message }} 
                </div>
            {% endfor %}
            {% for message in app.flashes('error') %}
                <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-3" role="alert">
                    {{ message }}
                </div>
            {% endfor %}
        </div>
        

        {% block body %}{% endblock %}

        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
        {% endblock %}
    </body>
</html>